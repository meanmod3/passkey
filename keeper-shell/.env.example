# Root env — copied into backend at runtime via docker-compose
DATABASE_URL="postgresql://keeper:keeper@localhost:5432/keepershell?schema=public"
JWT_SECRET="dev-only-change-me"
PORT=4000
FRONTEND_ORIGIN="http://localhost:5173"
NODE_ENV=development
